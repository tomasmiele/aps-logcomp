let x: int = 0;
let y: int;
const LIM: int = 5;
let flag: bool = true;

if (x < LIM) {
  x = x + 1;
} else if (doorOpen) {
  x = x + 2;
} else {
  x = x + 3;
}

while (x < 10) {
  y = temp;
  print(y);

  if (x == 3) {
    x = x + 1;
    continue;
  }

  if (x >= 8) {
    break;
  }

  x = x + 1;
}

for (let i: int = 0; i < 3; i = i + 1) {
  out(i);
}

when (temp < 40) {
  print(temp);
}

every 100 ms {
  print(clock_ms);
}

read(temp);